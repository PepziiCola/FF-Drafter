class Player {

    constructor(playerId, league, playerDetails) {
        this.playerId = playerId;
        this.league = league;
        this.playerDetails = playerDetails;
    }
}

module.exports = Player;